version: '3.7'

services:
  postgres:
    image: postgres:12-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./configs/postgres/.env
    secrets:
      - postgres_user
      - postgres_password
      - postgres_database
    networks:
      - codeshepherds-backend
  pgadmin:
    image: dpage/pgadmin4
    volumes:
      - pgadmin_data:/var/lib/pgadmin
      - pgadmin_data:/servers.json
    env_file:
      - ./configs/pgadmin/.env
    networks:
      - codeshepherds-backend
  django:
    build:
      context: ./web
      dockerfile: Dockerfile.prod
    image: localhost:5000/codeshepherds/django
    command: gunicorn codeshepherds.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
    secrets:
      - django_db_name
      - django_db_password
      - django_db_port
      - django_db_user
      - django_db_host
      - django_db_engine
    env_file:
      - ./configs/django/.env.prod
    depends_on:
      - postgres
    networks:
      - codeshepherds-backend
  nginx:
    image: nginx:1.17.5-alpine
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
      - ./configs/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - 80:80
    depends_on:
      - django
    networks:
      - codeshepherds-backend
      - codeshepherds-frontend

secrets:
  postgres_user:
    file: ./secrets/postgres_user
  postgres_password:
    file: ./secrets/postgres_password
  postgres_database:
    file: ./secrets/postgres_database
  django_db_name:
    file: ./secrets/django_db_name
  django_db_password:
    file: ./secrets/django_db_password
  django_db_port:
    file: ./secrets/django_db_port
  django_db_user:
    file: ./secrets/django_db_user
  django_db_host:
    file: ./secrets/django_db_host
  django_db_engine:
    file: ./secrets/django_db_engine

volumes:
  postgres_data: {}
  pgadmin_data: {}
  static_volume: {}
  media_volume: {}

networks:
  codeshepherds-backend:
  codeshepherds-frontend: